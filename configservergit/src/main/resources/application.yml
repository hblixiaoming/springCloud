server:
  port: 1201
spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/hblixiaoming/config-server/
          username: hblixiaoming
          password: lxm87085332LXM

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka/
